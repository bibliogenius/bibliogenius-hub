# Staging config (non-secret) for VPS deployment
# Copy to /opt/hub/.env.staging.config on the VPS
# Secrets are fetched from Scaleway Secret Manager at deploy time (see scripts/vps-deploy.sh)

APP_ENV=prod
APP_DEBUG=0

CORS_ALLOW_ORIGIN='^https?://(.*\.)?bibliogenius\.(org|app|fr)(:[0-9]+)?$'
DEFAULT_URI=https://hub-dev.bibliogenius.org

# Jira integration (non-secret parts)
JIRA_BASE_URL=https://biblio.atlassian.net
JIRA_PROJECT_KEY=KAN
JIRA_EMAIL=federico.calo@proton.me
